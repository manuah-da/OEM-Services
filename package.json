{
  "name": "oem-services-frontend",
  "version": "0.1.0",
  "description": "Independent promotion automation bundles for OEM pages.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "build:watch": "node build.mjs --watch",
    "check": "node scripts/check-build.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
